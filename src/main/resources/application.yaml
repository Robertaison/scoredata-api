server:
  port: 8081

spring:
  application:
    name: scoredata-api
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:mysql://localhost:3306/dataprovider
    username: root
    password: root
  rabbitmq:
    username: guest
    password: guest
    port: '5672'
    host: localhost